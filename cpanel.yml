---
deployment:
 tasks:
 - export DEPLOYPATH=/home/u2xoo5vmufch/public_html/alyfbaa/ems/
    - cp -R app $DEPLOYPATH
    - cp -R bin $DEPLOYPATH
    - cp -R bootstrap $DEPLOYPATH
    - cp -R config $DEPLOYPATH
    - cp -R database $DEPLOYPATH
    - cp -R public $DEPLOYPATH
    - cp -R resources $DEPLOYPATH
    - cp -R routes $DEPLOYPATH
    - cp -R screenshots $DEPLOYPATH
    - cp -R storage $DEPLOYPATH
    - cp -R tests $DEPLOYPATH
    - cp .env.example $DEPLOYPATH
   - cp artisan $DEPLOYPATH
   - cp composer.json $DEPLOYPATH
   - cp composer.lock $DEPLOYPATH
   - cp package.json $DEPLOYPATH
   - cp package-lock.json $DEPLOYPATH
   - cp phpunit.xml $DEPLOYPATH
   - cp readme.md $DEPLOYPATH
   - cp server.php $DEPLOYPATH
   - cp webpack.backend.mix.js $DEPLOYPATH
   - cp webpack.frontend.mix.js $DEPLOYPATH